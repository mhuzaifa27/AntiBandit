<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="15dp"
    android:background="@color/back_color">

    <TextView
        android:id="@+id/txt_alert_heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Anti Bandit Security Alert!!!"
        android:textColor="@color/white"
        android:textSize="25sp"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"/>
        <TextView
            android:id="@+id/txt_motion_dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enter Security password"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:layout_below="@+id/txt_alert_heading"
            android:layout_marginTop="30dp"
            android:layout_centerHorizontal="true"/>
    <EditText
        android:id="@+id/et_password"
        android:layout_below="@+id/txt_motion_dialog"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:layout_marginTop="25dp"
        android:layout_centerInParent="true"
        android:maxLength="20"
        android:hint="password here"
        android:inputType="textPassword"
        android:textColor="@color/back_color"/>

    <Button
        android:id="@+id/ok"
        android:layout_width="wrap_content"
        android:layout_below="@+id/et_password"
        android:layout_height="30dp"
        android:textColor="@color/back_color"
        android:background="@color/white"
        android:layout_marginTop="25dp"
        android:text="OK"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>